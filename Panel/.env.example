# Variables de entorno para Supabase
# Copia este archivo como .env.local y rellena los valores

# URL de tu proyecto Supabase (Dashboard > Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://tu-proyecto.supabase.co

# Anon Key (pública) - Dashboard > Settings > API > anon key
NEXT_PUBLIC_SUPABASE_ANON_KEY=tu-anon-key

# Service Role Key (privada, solo servidor) - Dashboard > Settings > API > service_role key
SUPABASE_SERVICE_ROLE_KEY=tu-service-role-key

# Bucket de storage (por defecto: media, alineado con la app principal)
NEXT_PUBLIC_SUPABASE_STORAGE_BUCKET=media

# Límite de peso de subida (MB) para validación en el API
NEXT_PUBLIC_MAX_UPLOAD_MB=8
